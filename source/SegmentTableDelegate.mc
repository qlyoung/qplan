import Toybox.Lang;
import Toybox.System;
import Toybox.WatchUi;

class SegmentTableDelegate extends ScrollableDelegate {
    function initialize(view as SegmentTableView) {
        ScrollableDelegate.initialize(view);
    }
}
